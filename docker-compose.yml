version: '3'

services:
  client:
    image: carousel:latest
    depends_on:
      - 'db'
    ports:
      - '3000:3000'

  db:
    image: mysql:latest
    restart: always
    command: --default-authentication-plugin=mysql_native_password
    environment:
      MYSQL_ROOT_PASSWORD: p4j4m4sII!
      MYSQL_DATABASE: hipsyimage
